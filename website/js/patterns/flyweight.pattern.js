/**
 * flyweight design pattern
 *
 * to optimize repetitive, slow and inefficiently shared data by sharing data as much as possible to consume mininal amount of memory
 *
 * share data or dom-layer (avoid attaching event handler for every child element in parent container)
 */
 ;(function(){


 })();
