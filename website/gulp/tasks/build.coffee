# Plugins requires

gulp = require 'gulp'
config = require '../config'
chalk = require 'chalk'
runSequence = require 'run-sequence'
fs = require 'fs'


gulp.task('build', () ->
  # config.destPath = config.path.servepath;
  # console.log(chalk.magenta.inverse('» Building for development...'));
  # runSequence('imgOptimise', 'watch');
)
